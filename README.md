# dotfiles

My personal dotfiles. Feel free to copy stuff or clone (though I can't ensure nothing will break 😅).


## WSL setup

under you Windows user directory add the file `.wslconfig` with the following content:

```ini
[wsl2]
networkingMode=mirrored
```
